apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik.dashboard
  namespace: kube-system
spec:
  dnsNames:
    - home.local
  secretName: home.local
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer